const FRAME_RATE = 10

function setup() {
  createCanvas(windowWidth, windowHeight);
  frameRate(FRAME_RATE)
}

function draw() {
  clear();
  background('#000');
  createCanvas(windowWidth, windowHeight);
}
